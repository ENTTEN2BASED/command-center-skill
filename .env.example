# CoinMarketCap API key — optional. Without it, regime runs on CoinGecko + Binance only.
# Get a free Basic key at https://coinmarketcap.com/api/
# Basic tier (10k credits/month) covers global-metrics and quotes — enough for regime enrichment.
CMC_API_KEY=your_key_here
